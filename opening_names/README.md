# Opening Names initialization

First you need to create the `all.tsv` file.
To do that, clone the [Lichess opening repository](https://github.com/lichess-org/chess-openings), and follow the instructions to build the `dist` folder.

Then, run `add_opening_names.py` in the container to add the names to the database.
